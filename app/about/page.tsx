"use client";
// import { motion } from "framer-motion";
import { useEffect, useRef, useState } from "react";

const AboutPage = () => {
  useEffect(() => {
    window.scrollTo(0, 0);
  }, [])
  return (
    <div>about</div>
  );
};

export default AboutPage;